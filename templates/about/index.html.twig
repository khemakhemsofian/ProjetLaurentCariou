{% extends 'base.html.twig' %}

{% block body %}
	{% for annonceAbout in aboutPage %}
		<div class="container about-page">
			<div class="row justify-content-center">
				<div class="col-md-4 col-lg-4 col-xs-12 col-sm-12">
					<img src={{asset('./upload/'~ annonceAbout.image)}} class="logo-about" alt="...">
					<h3 class="titre-about">About me</h3>
				</div>
				<div class="col-md-4 col-lg-4 col-xs-12 col-sm-12">
					<a class="text-about link-light">{{annonceAbout.description|format_file_from_text}}</a>
				</div>
			</div>
		</div>

	{% endfor %}

{% endblock %}
