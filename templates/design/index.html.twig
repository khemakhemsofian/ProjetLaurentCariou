{% extends 'base.html.twig' %}

{% block body %}

		{# <div id="carouselExampleControls" class="carousel slide" data-bs-ride="carousel">
			<div class="carousel-inner">
				<div class="carousel-item active">
               {% for AnnonceDesign in DesignList %}
							<div class="col">
								<img src={{asset('./upload/'~ AnnonceDesign.media)}} class="d-block w-30" alt="...">
							</div>
                {% endfor %}
				</div>
			</div>
			<button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleControls" data-bs-slide="prev">
				<span class="carousel-control-prev-icon" aria-hidden="true"></span>
				<span class="visually-hidden">Previous</span>
			</button>
			<button class="carousel-control-next" type="button" data-bs-target="#carouselExampleControls" data-bs-slide="next">
				<span class="carousel-control-next-icon" aria-hidden="true"></span>
				<span class="visually-hidden">Next</span>
			</button>
		</div> #}
		<div class="d-flex justify-content-center">
		 <div>
		 {% for AnnonceDesign in DesignPage %}
							<div class="col">
								<img src={{asset('./upload/'~ AnnonceDesign.media)}} class="d-block w-30" alt="...">
							</div>
         {% endfor %}
		 </div>
		{% do DesignPage.setPageRange(2) %}
		{{knp_pagination_render(DesignPage,'pagination.html.twig')}}
		</div>
{% endblock %}
