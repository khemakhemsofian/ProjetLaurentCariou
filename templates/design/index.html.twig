{% extends 'base.html.twig' %}

{% block body %}
	<div class="tous-les-designs">
		{% for AnnonceDesign in DesignPage %}
			<div class="images-des-designs"  id="carre{{loop.index}}">
				<a href={{path('design_single', {id:AnnonceDesign.id})}}><img src={{asset('./upload/'~ AnnonceDesign.media)}} alt="..."></a>
			</div>
		{% endfor %}
		{#{% do DesignPage.setPageRange(2) %}#}
		</div>
		<div class="navigation">
			{{knp_pagination_render(DesignPage,'pagination.html.twig')}}
		</div>
	
{% endblock %}
